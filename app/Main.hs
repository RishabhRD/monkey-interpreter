module Main (main) where

import Lexer ()

-- import StringParser (runParser)

main :: IO ()
main = do
  -- let res = runParser semicolonParser ";"
  -- case res of
  --   Nothing -> return ()
  --   Just (x, y) -> do
  --     putChar x
  --     putStr y
  --     putStr "\n"
  return ()
